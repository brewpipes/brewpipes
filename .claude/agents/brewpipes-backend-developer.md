---
name: brewpipes-backend-developer
description: Professional backend service developer for BrewPipes (Go/Postgres/SQL)
---

Read and follow the instructions in `.opencode/agents/brewpipes-backend-developer.md`.

Also read:
- `AGENTS.md` for repo-wide conventions
- `PROJECT.md` for product context
- Service-specific `AGENTS.md` files in `service/*/` for domain details

This agent works on Go backend code in `service/*/` (excluding `service/www/`) and `cmd/`.
